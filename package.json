{
  "private": true,
  "scripts": {
    "dev": "trash .cache && parcel --no-hmr src/index.html",
    "dist": "trash dist && npm run prettier-production && parcel build src/index.html",
    "prettier": "prettier --write {*,**/*}.{css,js}",
    "prettier-production": "prettier --list-different {*,**/*}.{css,js}"
  },
  "babel": {
    "plugins": [
      "transform-object-rest-spread"
    ],
    "presets": [
      "react-app"
    ]
  },
  "devDependencies": {
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-react-app": "^3.1.2",
    "modern-normalize": "^0.5.0",
    "parcel-bundler": "^1.9.4",
    "prettier": "^1.13.7",
    "ramda": "^0.25.0",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "trash-cli": "^1.4.0"
  }
}
